#pip install <package name> - cmd to install a package - pip is default python package manager
#https://pypi.org/ - we can dowbload specific packages from this link
import math

print (math.sqrt(25))

everything=dir(math) # to list everything from a module
print(everything)
# from module_name import function_name e.g: from math import sqrt - imports only sepecific fucntion from given module - print(sqrt(ss)) can be used 
